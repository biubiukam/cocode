engine-strict=true
package-manager-strict=true
# Kept in sync with cocode-gui, which stages this workspace into the release
# runtime. Switching linkers under an existing tree leaves dangling store links
# behind in the workspace packages' node_modules.
node-linker=hoisted
