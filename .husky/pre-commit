if git diff --cached --name-only -- cocode-gui | grep -q .; then
  pnpm --dir cocode-gui exec lint-staged --cwd . --config lint-staged.config.mjs
fi
if git diff --cached --name-only -- cocode-tui | grep -q .; then
  pnpm --dir cocode-tui exec lint-staged --cwd . --config lint-staged.config.mjs
fi
